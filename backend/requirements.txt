# FastAPI & Core
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-dotenv==1.0.0
pydantic==2.10.0

# AI/ML APIs
openai==1.54.0
pinecone==5.4.0

# Audio processing
python-multipart==0.0.6
ffmpeg-python==0.2.0
soundfile==0.12.1

# Speech recognition - openai-whisper only (whisperx removed to avoid torch version conflicts)
openai-whisper==20231117

# PyTorch - MUST install before pyannote.audio
torch==2.2.0
torchaudio==2.2.0

# Numerical libraries - compatible with torch 2.2.0 and openai-whisper
numpy==1.26.4
scipy==1.11.4

# Speaker diarization - installed last so it can use existing torch
pyannote.audio==3.3.2
